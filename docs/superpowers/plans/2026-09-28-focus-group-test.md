# Тест с фокус-группой на `pmo-test`

> Ход работ: план на согласовании (2026-09-25), кросс-ревью проведено, замечания учтены.

**Цель:** довести портал на `/sites/pmo-test` до состояния, в котором фокус-группа работает с ним в браузере на компьютере, планшете и телефоне (iOS и Android) по инструкции, а замечания собираются в приложении. Прод, Teams и каталог тенанта — после разбора замечаний, отдельным решением.

**Решения (2026-09-25):** замечания — кнопка «Відгук» в приложении → список «Відгуки»; инструкция — «Довідка» в приложении + документ в репозитории + страница для рассылки; доступ — роли участников в демо-проектах (права выдаёт синхронизация); Teams на тесте не нужен.

**Основа:** [SPEC.md](../../SPEC.md), [этап 4](2026-09-27-spfx-phase4-access-teams-prod.md) — задачи 1, 2, 4 оттуда входят сюда; задачи 3, 5, 6 (Teams, прод) — после фокус-группы.

## Модель прав (решение 2026-09-25)

| Кто | Видит | Может |
|---|---|---|
| PM проекта | свой проект | править карточку, сдавать статус-отчёты, вести риски, комментировать |
| Руководители PM по цепочке Entra ID (руководитель PM-ов, CIO, CEO…) | проекты подчинённых | смотреть и комментировать |
| Власник, стейкхолдери и их руководители по цепочке | проекты, где у них или подчинённых есть роль | смотреть и комментировать |
| `PMO-адміністратори` | все проекты | создавать новые проекты (и назначать PM); остальное — смотреть и комментировать; править — только как PM своих |
| Владельцы сайта | всё | администрирование (полный доступ SharePoint) |
| Остальные сотрудники | пустой портал | — |

Архивный проект — только просмотр для всех, включая PM.

Было: редактировали PM, руководители PM и собственник; проект мог создать любой участник сайта; PMO — полный доступ.

## Задачи

### Задача 0. Новая модель прав

**Файлы:** `scripts/Invoke-PMOSync.ps1` (ACL проекта и записей: PM — edit, остальные и PMO — read; отчёт применяется, только если автор — PM проекта или владелец сайта, иначе предупреждение в журнале синхронизации и отчёт остаётся неприменённым), `scripts/Deploy-PMO.ps1` (права списков: «Проєкти» — участникам сайта чтение, PMO — добавление; «Статус-звіти», «Ризики», «Коментарі» — участникам добавление; PMO-группа без полного доступа к элементам), приложение (`canCreate` по праву `AddListItems` на «Проєкти» — кнопка «Новий проєкт» только у PMO; тексты «Змінювати проєкт може лише PM…»; `overlay.ts` не накладывает отчёты не от PM), общие векторы `tests/cases/acl.json`, прототип (`canEdit`, `accessList`, тексты), `docs/SPEC.md`, `docs/DEPLOYMENT.md`, `CLAUDE.md`.

**Приёмка:** векторы прав проходят в PowerShell и Jest; на `pmo-test` после синхронизации: PM правит свой проект, его руководитель и собственник — только смотрят и комментируют, у не-PMO нет «Новий проєкт».


### Задача 1. Доступ в карточке

Задачи 1 и 2 этапа 4 с новой моделью прав (правка — только PM; строка PMO — «перегляд усіх проєктів»): поле `pmAccess`, его заполнение синхронизацией в проходе прав, общие векторы `tests/cases/access.json`, раздел «Доступ до картки» в приложении.

### Задача 2. Телефон и планшет

**Файлы:** `theme/overrides.scss` (раздел `@media`), при необходимости `components/*` (атрибуты, классы), `pages/*`.

Ширины: телефон ≤ 600 px, планшет 600–1024 px, книжная и альбомная ориентация. Правила прототипа (`@media` 520 / 560 / 680 / 720 / 920 / 1100) переносятся полностью, дальше — дополнения:
- нигде нет горизонтальной прокрутки страницы; прокручивается только таблица внутри своей рамки, колонка названия закреплена слева;
- панель инструментов списков (Список / Плитки, представление, колонки) переносится по строкам, выбор представления — на всю ширину;
- на телефоне «Проєкти» по умолчанию открываются плитками, если пользователь не выбирал сам;
- карточка и формы — на весь экран, крестик и кнопки сохранения всегда видны, отступы под «чёлку» и полосу жестов (`env(safe-area-inset-*)`);
- формы в одну колонку; поля ввода не меньше 16 px (iOS не увеличивает страницу при вводе), кнопки и цели касания от 40 px;
- всплывающие фильтры и выбор колонок помещаются в экран (на телефоне — снизу на всю ширину);
- шапка: вкладки прокручиваются, язык и тема — компактно;
- главная: кольцо и динамика друг под другом, блоки-таблицы — прокрутка внутри.

**Проверка:** рамка — только быстрый поиск поломок вёрстки; приёмку даёт реальное устройство (зум полей, safe-area, клавиатура в рамке не воспроизводятся). В Chrome — приложение во вложенной рамке 360, 390, 768, 820, 1024 px (рамка того же сайта сохраняет вход), каждый экран, карточка и все формы; скрипт ищет элементы шире экрана — 0 найденных. На реальных устройствах — ты: iPhone (Safari и приложение SharePoint), Android (Chrome и приложение SharePoint), iPad. **Первым делом** — открыть `Portal.aspx` в приложении SharePoint на телефоне: страницы-приложения SPFx оно может показывать ограниченно; если не показывает — в инструкции «открывать в браузере», в приложении SharePoint — ссылка на браузер.

### Задача 3. Кнопка «Відгук» и список «Відгуки»

**Файлы:** `scripts/Deploy-PMO.ps1` (список `Lists/Feedback`: текст, экран (адрес `#…`), устройство (ширина экрана, браузер), статус разбора: Новий / Прийнято / Відхилено / Зроблено, ответ; автор и дата — стандартные; участники сайта могут добавлять; настройки списка `ReadSecurity = 2` и `WriteSecurity = 2` — каждый видит и правит только свои, PMO-адміністратори (полный доступ) — все), `data/SpRepo.ts`, `components/Header.tsx`, `panels/FeedbackForm.tsx` (новый), `i18n/strings.ts`.

- Кнопка в шапке, форма в панели: «Що не так або що покращити?», экран подставляется сам. После сохранения — уведомление.
- **Скриншоты:** до 5 изображений на отзыв (вложения элемента списка, до 10 МБ каждое). На компьютере — вставка из буфера (Ctrl/Cmd+V после снимка экрана) и выбор файла; на телефоне и планшете — выбор из галереи или камера (`accept="image/*"`). Превью с удалением до отправки; большие снимки сжимаются в браузере до 1920 px по длинной стороне.
- Показывается, только если на сайте есть список «Відгуки» (его создаёт `Deploy-PMO.ps1` с ключом `-Feedback`, `Invoke-Env` передаёт его только для `test`), — на проде кнопки нет, пока список не создан решением.
- Выгрузка для разбора — стандартное представление списка или `Export`.

**Приёмка:** отзыв со скриншотами с телефона (iOS, Android) и с компьютера (вставка из буфера) попадает в список с экраном, устройством и вложениями; другой участник чужих отзывов не видит.

### Задача 4. «Довідка» и инструкция

**Файлы:** `docs/USER-GUIDE.uk.md`, `docs/USER-GUIDE.en.md`, `docs/USER-GUIDE.ru.md` (новые), `panels/Help.tsx` (новый), `components/Header.tsx`, страница для рассылки (артефакт claude.ai).

Содержание: что такое портал; как открыть (компьютер, телефон, приложение SharePoint, добавить на главный экран); роли и что видит каждый; главная; проекты (плитки, список, фильтры, колонки, представления); карточка; новый проект; статус-отчёт (оценки, общий стан, ключевые показатели и причина, «Завершено»); риск; комментарий; архив; язык и тема; как оставить отзыв; **сценарии для фокус-группы** — 8–10 заданий «сделайте и оцените» по ролям. Три языка — UA / EN / RU; в приложении «Довідка» показывает текст на выбранном языке интерфейса.

**Приёмка:** текст сверен с приложением по каждому экрану (сверка «пункт инструкции → экран»: 0 расхождений).

### Задача 5. Синхронизация на `pmo-test`

Задача 4 этапа 4: ты регистрируешь PMO Sync (`-DeviceLogin`, вход в Chrome), дальше `sync-dryrun` → разбор → `sync` → проверка сценариев 1–9 из SPEC §12.

### Задача 6. Фокус-группа в демо-проектах

**Файлы:** `scripts/Seed-TestData.ps1` — новый ключ `-Roles config/focus-group.json` (не в git: e-mail → PM / власник / стейкхолдер по кодам `TEST-NN`). Обычный `seed` пропускает существующие проекты целиком, поэтому роли назначаются отдельным проходом: для каждого проекта из файла `pmManager` / `pmOwner` / `pmStakeholders` перезаписываются, если отличаются (повторный запуск ничего не меняет), проекты не пересоздаются. `.gitignore` — `config/focus-group.json`; после теста файл удаляется (в нём e-mail сотрудников).

- Участники — в «Учасники сайта»; каждому минимум одна роль PM, одна власника и одна стейкхолдера, чтобы увидеть разные права; один-два человека без ролей — проверить, что чужие проекты не видны.
- Руководители в Entra ID — как есть (права по иерархии проверятся сами).
- После — `sync`: права и «Доступ до картки».

**Приёмка:** под учётной записью участника (проверяет он сам) видны только его проекты, правка — где он PM / власник.

### Задача 7. Приёмка перед выдачей

Полная сверка: сценарии SPEC §12 + задачи 2–4 на `pmo-test`, версия приложения, запись в CHANGELOG, `README` («Состояние»), коммит и push после «да». Выдача фокус-группе — ты.

## Порядок

0 → 1 (код) и 5 (твоя регистрация) — параллельно → 2 → 3 → 4 (инструкция пишется по готовому интерфейсу) → 6 (нужны 5 и список людей) → 7.

## Что нужно от тебя

1. Зарегистрировать PMO Sync для `pmo-test` — команда будет в ответе.
2. Список фокус-группы: e-mail, кто в какой роли (или «раздай сам» — распределю).
3. Проверка на реальных телефонах и планшете после задачи 2.
