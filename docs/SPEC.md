# Портфель проєктів — техническое задание

Версия ТЗ: 2026-09-25. Эталон интерфейса — прототип [`prototype/pmo-prototype.html`](../prototype/pmo-prototype.html) (описание — [PROTOTYPE.md](PROTOTYPE.md)).
Если текст ТЗ и прототип расходятся, решает прототип, кроме пунктов, явно помеченных «отличие от прототипа».

## 1. Назначение

Единое место, где компания ведёт портфель проектов:
- у каждого проекта — карточка: участники, сроки, бюджет, состояние, история изменений;
- PM регулярно сдаёт статус-отчёт — только через него меняются ключевые показатели проекта;
- риски и проблемы проекта с оценкой «вероятность × влияние»;
- руководство видит состояние портфеля на главной: сводку, динамику, проблемные проекты, запросы на решение;
- каждый видит только те проекты, в которых участвует он сам или его подчинённые.

## 2. Пользователи и роли

| Роль | Кто | Что может |
|---|---|---|
| PM | ответственный за проект (поле «PM») | редактировать карточку, сдавать статус-отчёты, вести риски, комментировать |
| Руководители PM | цепочка руководителей PM по полю Manager в Entra ID | как PM |
| Власник (собственник) | заказчик проекта | как PM |
| Стейкхолдери | несколько человек | просмотр и комментарии |
| Руководители собственника и стейкхолдеров | цепочки в Entra ID | просмотр и комментарии |
| `PMO-адміністратори` и владельцы сайта | офис управления проектами | всё, во всех проектах, включая архив |
| Остальные сотрудники | участники сайта | создать новый проект; чужие проекты не видят |

Проект в архиве — только просмотр для всех, кроме PMO и владельцев сайта.

## 3. Где работает

- **SharePoint Online**, сайт-коммуникация `/sites/pmo` (тестовый — `/sites/pmo-test`). Главная страница — приложение SPFx на весь экран (страница приложения из одной веб-части).
- **Microsoft Teams** — то же приложение как личное приложение / вкладка (этап 4).
- Стандартные списки SharePoint остаются: для администраторов, выгрузок в Excel и Power BI.

## 4. Модель данных

Внутренние имена полей не переименовываются никогда; меняются только отображаемые названия (uk / en / ru).

### 4.1. Проєкти (`Lists/Projects`)

| Поле | Имя | Тип | Правка |
|---|---|---|---|
| Назва проєкту | `Title` | текст, обязательно | карточка |
| Код | `pmCode` | текст; пусто → следующий `PRJ-###` | карточка |
| Напрям | `pmDepartment` | выбор: ІТ, HR та кадрове адміністрування, Розрахунок зарплати, Продажі, Фінанси, Операції, Юридичний | карточка |
| Посилання на Loop | `pmLoop` | ссылка | карточка |
| Тип | `pmType` | Стратегічний / Звичайний | при создании, дальше — отчёт |
| Пріоритет | `pmPriority` | 1 — Високий / 2 — Середній / 3 — Низький | карточка |
| PM | `pmManager` | пользователь, обязательно | карточка |
| Власник | `pmOwner` | пользователь | карточка |
| Стейкхолдери | `pmStakeholders` | несколько пользователей | карточка |
| Статус | `pmStatus` | Ініціація, Планування, Реалізація, Призупинено, Скасовано, Архівний | при создании, дальше — отчёт |
| Загальний стан | `pmRAG` | Зелений / Жовтий / Червоний | только отчёт |
| % виконання | `pmProgress` | 0–100 | только отчёт |
| Дата старту / запуску (продакшн) / завершення (план) | `pmStart`, `pmGoLive`, `pmPlanEnd` | дата | при создании, дальше — отчёт |
| Прогноз завершення | `pmForecastEnd` | дата | только отчёт |
| Дата архівації | `pmArchivedAt` | дата | синхронизация |
| Бюджет (план) / Фактичні витрати | `pmBudget`, `pmActualCost` | деньги, ₴ | бюджет — карточка; факт — отчёт |
| Останній статус-звіт / Останній апдейт | `pmLastReport`, `pmLastUpdate` | дата / текст | синхронизация |
| Останній коментар | `pmLastComment` | текст | синхронизация |
| Опис | `pmDescription` | многострочный | карточка |
| служебные | `pmoAcl` (хэш выданных прав), `pmEditLog` (правки карточки до переноса в журнал) | скрыты | приложение / синхронизация |

### 4.2. Статус-звіти (`Lists/StatusReports`)

Проект, дата отчёта, период (Тиждень, 2 тижні, Місяць, Квартал); три оценки — терміни `srSchedule`, бюджет `srBudget`, ресурси `srResources`; вычисляемое общее состояние `srRAG`; новые значения ключевых показателей (`srStatus`, `srType`, `srProgress`, `srStart`, `srGoLive`, `srPlanEnd`, `srForecastEnd`, `srActualCost`) и причина их изменения `srKeyReason`; резюме одной строкой `Title`; зроблено / план / проблеми (`srDone`, `srNext`, `srIssues`); «Потрібне рішення керівництва» `srDecision` и текст решения `srDecisionText`; копии типа и приоритета проекта; служебные `srApplied` (перенесён ли в карточку), `pmoAcl`. В статусе отчёта есть дополнительное значение «Завершено» — оно отправляет проект в архив.

### 4.3. Ризики та проблеми (`Lists/RisksIssues`)

Проект, опис `Title`, тип (Ризик / Проблема), ймовірність 1–5, вплив 1–5, вычисляемая оцінка = P × I, власник ризику, статус (Відкрито, В роботі, Закрито), термін, заходи реагування, копии типа и приоритета проекта.

### 4.4. Служебные списки

- **Зміни показників** (`Lists/KeyChanges`) — журнал: проект, дата, кто изменил, вид изменения (Створення, Статус-звіт, Редагування картки), поле, було, стало, причина. Пользователям — только чтение.
- **Коментарі** (`Lists/ProjectComments`) — проект и текст; автор и дата — стандартные поля.

Дочерние списки связаны с проектом подстановкой с запретом удаления.

## 5. Бизнес-правила

1. **Ключевые показатели** — статус, общее состояние, тип, % выполнения, все даты, фактические затраты — меняются только через статус-отчёт. В форме карточки их нет (при создании проекта тип, статус и даты задаются).
2. **Общее состояние отчёта** — худшая из трёх оценок: хотя бы одна красная → Червоний; иначе хотя бы одна жёлтая → Жовтий; иначе Зелений. Вручную не выбирается. Состояние проекта = состояние самого свежего по дате отчёта.
3. **Перенос отчёта в карточку.** Каждый ещё не применённый отчёт переносит в проект заполненные ключевые показатели. Самый свежий отчёт задаёт ещё общее состояние, дату последнего отчёта и «Останній апдейт» (резюме). Отчёт помечается применённым.
4. **Причина обязательна**, если отчёт меняет статус, тип или любую дату. Она попадает в журнал.
5. **Завершение.** Статус «Завершено» в отчёте → проект получает «Архівний» и дату архивации, уходит в «Архів», становится только для чтения.
6. **Журнал.** Каждое изменённое поле — строка «було → стало», кто, когда, причина. Создание проекта — строка «Створення». Правка карточки — «Редагування картки».
7. **Свежесть отчёта:** до 8 дней — зелёный, 8–14 — жёлтый, больше 14 — красный, нет отчётов — серый.
8. **Просрочка:** дата завершения (план) раньше сегодняшней у активного проекта — красным.
9. **Отклонение прогноза** — разница «прогноз − план» в днях, красным при сдвиге вправо.
10. **Освоение бюджета** = факт / план: больше 90% — жёлтый, больше 100% — красный.
11. **Оценка риска** = вероятность × влияние: 15–25 высокий (красный), 8–14 средний (жёлтый), 1–7 низкий (зелёный). Шкалы:

    | Балл | Ймовірність | Вплив |
    |---|---|---|
    | 1 | рідко, до 10% | незначний: не влияет на сроки и бюджет |
    | 2 | малоймовірно, 10–30% | помірний: отклонение в пределах резерва |
    | 3 | можливо, 30–60% | суттєвий: сдвиг этапа или перерасход до 10% |
    | 4 | ймовірно, 60–90% | значний: сдвиг запуска или перерасход более 10% |
    | 5 | майже напевно, более 90% | критичний: срыв целей проекта |

12. **Активный проект** — любой статус, кроме «Скасовано», «Архівний», «Завершено».
13. **Код проекта:** если не введён — `PRJ-` + (максимальный номер среди `PRJ-###` + 1), три цифры.
14. **Порядок колонок** во всех таблицах: стратегический (щит с мечом; заголовок — иконка компаса), приоритет (стрелки ⏫ ＝ ⏬; заголовок — флажок), название, остальные.
15. **Сортировка по умолчанию** (отличие от прототипа, по итогам приёмки): новые записи сверху — проекты и риски по дате создания, статус-отчёты по дате отчёта, затем по созданию. Любую колонку можно отсортировать щелчком по заголовку.
16. Группировок по статусам нет.

## 6. Интерфейс

Общие элементы (см. [PROTOTYPE.md](PROTOTYPE.md)):
- **Шапка:** логотип и название «Портфель проєктів», вкладки Головна · Проєкти · Статус-звіти · Ризики та проблеми · Архів, переключатель языка UA / EN / RU, светлая / тёмная тема, аватар текущего пользователя.
- **Язык** по умолчанию — язык профиля Microsoft 365 (uk / en / ru, иначе uk); выбор запоминается в браузере. Значения выбора и названия представлений — на украинском во всех языках.
- **Тема** по умолчанию — системная; выбор запоминается.
- **Адрес** `#вкладка/представление/id/форма` открывает нужную вкладку, представление, карточку или форму — ссылку можно переслать.
- **Панель** справа: карточка проекта и все формы; закрывается крестиком, Esc или щелчком по фону.
- **Уведомление** (toast) после сохранения.

### 6.1. Головна

- заголовок «Панель проєктів», «Доступно вам проєктів: N», кнопка «Новий проєкт»;
- «Портфель за станом» — кольцо по общему состоянию активных проектов с числом в центре и легендой (зелений, жовтий, червоний, не оцінено);
- «Динаміка стану портфеля» — столбцы с разбивкой по цвету, 7 срезов через 14 дней до сегодня; на дату среза берётся последний отчёт проекта, архивный проект учитывается до даты архивации;
- блоки во всю ширину, каждый со ссылкой «Показати все»: «Проблемні проєкти» (жёлтые и красные), «Немає свіжого статус-звіту» (больше 14 дней или нет отчётов), «Потребують рішення керівництва», «Відкриті ризики».

Всё считается по проектам, видимым пользователю.

### 6.2. Проєкти

- представления: Усі проєкти, Стратегічні, Проблемні, Мої проєкти (я PM, власник или стейкхолдер), Немає свіжого звіту;
- режимы «Плитки» и «Список»;
- плитка: код, щит стратегического проекта, состояние плашкой, название, статус и % с полосой, «Останній апдейт» со светофором свежести, ссылка на Loop, PM;
- таблица: выбор колонок (шестерёнка), сортировка и фильтр (воронка) по каждой колонке, счётчик «Показано N з M»;
- колонки: стратегічний, пріоритет, назва, код, тип, PM, власник, стейкхолдери, статус, стан, дата звіту, давність звіту, % виконання, дати старту / запуску / плану / прогнозу, відхилення, бюджет, факт, освоєння, останній апдейт, останній коментар, автор коментаря, напрям, Loop, дата архівації;
- подсказка о красной дате и светофоре отчёта.

### 6.3. Статус-звіти

Представления «Усі звіти» и «Потребують рішення»; кнопка «Новий статус-звіт»; колонки: стратегічний, пріоритет, проект, код, дата, період, стан, три оценки, резюме, зроблено, план, проблеми, %, статус, автор, «Потрібне рішення» (при наведении — текст решения), текст рішення.

### 6.4. Ризики та проблеми

Представления «Відкриті» (не закрытые) и «Усі елементи»; кнопка «Новий ризик»; колонки: проект, пріоритет, стратегічний, код, опис, тип, оцінка (цветная плашка), ймовірність, вплив, власник, статус, термін (красный, если прошёл у открытого риска), заходи. Подсказка «Оцінка = ймовірність × вплив».

### 6.5. Архів

Проекты в статусе «Архівний» с датой архивации; карточка — только просмотр.

### 6.6. Карточка проекта

Сверху: код, список, название; плашки — тип, состояние, статус, приоритет, «Відкрити в Loop»; кнопки «Додати статус-звіт», «Редагувати» (если есть право), иначе замок «Змінювати проєкт можуть PM, власник, керівники PM і PMO…».

Разделы:
1. **Учасники** — PM крупно (должность, почта), власник, стейкхолдери.
2. **Терміни** — старт, запуск, план, прогноз с отклонением в днях.
3. **Бюджет і виконання** — % с полосой, последний отчёт со светофором, бюджет, факт, освоение.
4. **Історія змін** — последние 3 события, «Показати всю історію (N)»: кто, когда, вид («Оновлено статус-звітом», «Редагування картки», «Створення»), поля «було → стало», причина и резюме отчёта. Сохранённый только что отчёт и правка карточки видны сразу, до синхронизации.
5. **Коментарі** — лента и форма «Додати коментар».
6. **Історія станів** — матрица по последним 8 отчётам: общий стан, терміни, бюджет, ресурси, %.
7. **Статус-звіти** — отчёты проекта: дата, стан, резюме, зроблено / план / проблеми, «Потрібне рішення».
8. **Ризики та проблеми** — риски проекта с оценкой; «Додати ризик».

### 6.7. Формы

**Новий проєкт / Редагування.** Разделы «Загальне», «Учасники», «Терміни» (только при создании), «Бюджет і виконання». Обязательны название и PM; PM нового проекта по умолчанию — текущий пользователь. Код — подсказка «автоматично». При правке — заметка, что доступ пересчитается. Правка сохраняет «було → стало» для журнала.

**Статус-звіт.** Проект (только активные, где есть право), дата (сегодня), период; три оценки обязательны; «Загальний стан (розраховується)» пересчитывается на лету; блок «Ключові показники» заранее заполнен текущими значениями проекта — статус (+ «Завершено → в архів»), %, тип, четыре даты, фактичні витрати; при изменении статуса, типа или дат появляется обязательное «Причина зміни»; резюме одной строкой (обязательно), зроблено, план, проблеми, флажок «Потрібне рішення керівництва» и текст решения.

Сообщения об ошибках: «Оцініть терміни, бюджет і ресурси», «Заповніть резюме одним рядком», «Вкажіть дату звіту», «Вкажіть причину зміни», «Вкажіть назву проєкту», «Оберіть відповідального PM».

**Ризик.** Проект (фиксирован, если открыт из карточки), опис (обязательно), тип, шкалы 1–5 с описанием балла, живая оценка «15 Високий = 3 × 5», власник ризику (по умолчанию PM), статус, термін, заходи. Без права — просмотр с замком.

**Коментар.** Текст обязателен.

Выбор людей — поиск по каталогу пользователей (имя, должность), несколько значений для стейкхолдеров.

## 7. Права доступа

Права выдаёт синхронизация на проект и все его записи (отчёты, риски, комментарии, журнал):

| Кто | Проект, отчёты, риски | Комментарии и журнал |
|---|---|---|
| PM, руководители PM, собственник | редактирование | чтение, добавление комментариев |
| Стейкхолдеры, руководители собственника и стейкхолдеров | просмотр | то же |
| `PMO-адміністратори`, владельцы сайта | полный доступ | полный доступ |

Права пересчитываются, только если изменился круг участников (сравнение по хэшу в `pmoAcl`); раз в неделю — полный пересчёт, чтобы учесть смену руководителей. Приложение показывает кнопки правки по фактическим правам элемента, само прав не вычисляет.

## 8. Разделение ответственности

| Кто | Что пишет |
|---|---|
| Приложение SPFx (от имени пользователя) | новые проекты; правку карточки без ключевых показателей + `pmEditLog`; статус-отчёты (`srApplied = нет`); риски; комментарии |
| Синхронизация `Invoke-PMOSync.ps1` (по расписанию, раз в 10–15 минут) | перенос отчёта в карточку, архив, журнал, последний комментарий, копии типа и приоритета, права, напоминания PM |

На экране результат сохранения виден сразу: приложение накладывает неприменённые отчёты на карточку по тем же правилам, что и синхронизация. В стандартных списках изменения появляются после ближайшей синхронизации. Одинаковость правил в PowerShell и TypeScript проверяют общие тест-векторы `tests/cases/*.json`.

## 9. Напоминания

По ключу `-SendReminders` (раз в неделю): каждому PM одно письмо со списком его активных проектов без отчёта дольше `-ReminderDays` дней (по умолчанию 7). Сейчас не включены.

## 10. Нефункциональные требования

- Три языка интерфейса; светлая и тёмная темы; адаптивная вёрстка.
- Данные — только в списках SharePoint сайта; внешних сервисов и баз нет.
- Приложение работает от имени пользователя: видит только то, что ему открыто.
- Скрипты идемпотентны: повторный запуск ничего не дублирует. Данные не теряются: удаление или смена типа поля — только миграцией с переносом значений.
- Прод обновляется только после явного подтверждения.

## 11. Этапы

| Этап | Содержание | Состояние |
|---|---|---|
| 0 | Списки, поля, синхронизация, права, развёртывание скриптами | готово |
| 1 | Каркас SPFx, шапка, язык, тема, главная | готово, `pmo-test` |
| 2 | Проекты (плитки, таблица, фильтры, колонки, представления), отчёты, риски, архив, карточка (чтение) | готово, `pmo-test` |
| 3 | Формы: проект, правка карточки, статус-отчёт, риск, комментарий | готово, `pmo-test` |
| 4 | Раздел «Доступ» в карточке, пакет Teams, регистрация синхронизации, выпуск на `/sites/pmo` | впереди |

Планы этапов — [`docs/superpowers/plans/`](superpowers/plans/), проектное решение по приложению — [`docs/superpowers/specs/2026-09-24-spfx-portal-design.md`](superpowers/specs/2026-09-24-spfx-portal-design.md).

## 12. Сценарии приёмки

1. Новый проект без кода → получает `PRJ-###`, PM — я, появляется первым в «Проєкти».
2. Правка карточки (приоритет) → в «Історії змін» сразу «Редагування картки: 2 — Середній → 1 — Високий».
3. Статус-отчёт со сдвигом плана без причины → ошибка; с причиной → карточка сразу показывает новую дату, в истории «Оновлено статус-звітом».
4. Статус-отчёт «Завершено» → проект уходит в «Архів», карточка только для чтения.
5. Риск 5 × 4 → оценка 20, красная плашка «Високий».
6. Комментарий → появляется в карточке.
7. Пользователь без права правки → кнопок правки нет, замок с пояснением.
8. Переключение UA / EN / RU и светлой / тёмной темы — интерфейс переведён полностью.
9. После синхронизации: отчёт помечен применённым, журнал заполнен, права выданы, архивный проект — только чтение.
